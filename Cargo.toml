[package]
name = "rps"
version = "0.1.0"
edition = "2024"

[dependencies]
lazy_static = "1.5.0"
rand = "0.9.0"
rayon = "1.10.0"
weighted_rand = "0.4.2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "new_benchmark"
harness = false
